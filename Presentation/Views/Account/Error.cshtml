<div class="pt-5"></div>
<div class="pt-5"></div>
<div class="pt-5"></div>

<h1 class="text-center p-3">Erro</h1>

<div class="alert alert-danger">
    <p>Ocorreu um erro durante a sua solicitação.</p>
    <ul>
        @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
        {
            <li>@error.ErrorMessage</li>
        }
    </ul>
</div>
